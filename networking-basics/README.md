# networking-basics

Creates a baseline report of:
- interfaces and routes
- DNS resolver config
- listening ports
- basic tests: ping, DNS lookup, HTTP HEAD, traceroute (if installed)

Goal: build intuition for troubleshooting and common ports/protocols.
